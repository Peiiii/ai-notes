# Provider selection
# Use 'gemini' or 'openai'. Default is 'gemini' if not set.
VITE_AI_PROVIDER=gemini

# ======================
# Gemini configuration
# ======================
# Required when VITE_AI_PROVIDER=gemini
# Note: In a Vite frontend app, only variables prefixed with VITE_ are exposed to the client.
VITE_GEMINI_API_KEY=

# Optional: override default model
# Common choices: gemini-2.5-flash, gemini-2.0-pro-exp, etc.
VITE_GEMINI_MODEL=gemini-2.5-flash

# ======================
# OpenAI configuration
# ======================
# Required when VITE_AI_PROVIDER=openai
VITE_OPENAI_API_KEY=

# Optional: override default model
# Examples: gpt-4o-mini, gpt-4o, o4-mini
VITE_OPENAI_MODEL=gpt-4o-mini

# Optional: for self-hosted or compatible endpoints (e.g., Azure/OpenAI-compatible servers)
VITE_OPENAI_BASE_URL=

# If set to '1', force response_format: {type: 'json_object'} even on non-openai base URLs
VITE_OPENAI_FORCE_JSON=

# ======================
# Generic optional overrides
# ======================
# If set, these override the provider-specific model/base url.
# Typically you don't need these unless unifying config across providers.
VITE_AI_MODEL=
VITE_AI_BASE_URL=

# ----------------------
# Non-VITE variants (server-side or proxy use only)
# ----------------------
# These will NOT be available to browser code unless explicitly injected.
# Only use them if you add a server/proxy layer.
# AI_PROVIDER=
# GEMINI_API_KEY=
# GEMINI_MODEL=
# OPENAI_API_KEY=
# OPENAI_MODEL=
# OPENAI_BASE_URL=
